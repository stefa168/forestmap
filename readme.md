# Dipendenze

Per la gestione delle immagini, questo server si basa su `libvips` e **non** potrà essere avviato senza di essa.

Le istruzioni per l'installazione (basta `sudo apt install libvips`) sono presenti
[qui](https://github.com/libvips/libvips/wiki/Build-for-Ubuntu).